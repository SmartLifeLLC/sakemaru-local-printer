# Sakemaru ローカルプリンターシステム セットアップガイド

## 目次

1. [システム要件](#システム要件)
2. [初回セットアップ](#初回セットアップ)
3. [倉庫別プリンター設定](#倉庫別プリンター設定)
4. [運用開始](#運用開始)
5. [トラブルシューティング](#トラブルシューティング)

---

## システム要件

- **OS**: Windows 10/11（推奨）、macOS、Linux
- **Node.js**: v18以上
- **ネットワーク**: サーバーへのHTTPS接続が必要
- **プリンター**: システムに認識されているプリンター

---

## 初回セットアップ

### 1. アプリケーションのインストール

1. インストーラーを実行
2. 初回起動時、自動的に設定画面が表示されます

### 2. 基本設定

#### 2.1 API接続設定

1. **APIホスト**を入力
   - 例: `tani-hub.sakemaru.click`
   - `https://`は不要です

2. **APIトークン**を入力
   - サーバー管理者から提供されたトークンを入力

3. **API接続テスト**をクリック
   - 接続が成功すれば✅マークが表示されます
   - 失敗する場合は、ホスト名とトークンを確認してください

#### 2.2 S3設定

PDFファイルをダウンロードするためのS3設定を行います：

- **Bucket**: S3バケット名
- **Region**: `ap-northeast-1`（通常）
- **AccessKeyId**: アクセスキー
- **SecretAccessKey**: シークレットキー

これらの情報は、サーバー管理者から提供されます。

#### 2.3 プリンター設定

1. **「プリンタ一覧を読み込み」**ボタンをクリック
2. システムに認識されているプリンターが一覧表示されます
3. **プリンタ1〜4**に使用するプリンターを割り当てます

**プリンター番号について:**
- **プリンタ1 (printer0)**: デフォルトプリンター（必須）
- **プリンタ2 (printer1)**: 倉庫Aの伝票用
- **プリンタ3 (printer2)**: 倉庫Bの伝票用
- **プリンタ4 (printer3)**: 予備

**重要:** サーバー側で設定する`printer_id`（0-3）と対応させる必要があります。

---

## 倉庫別プリンター設定

### 概要

倉庫ごとに異なるプリンターで印刷する場合の設定方法です。

### サーバー側の設定（管理者向け）

倉庫ごとにプリンター設定を登録します：

```bash
# 東京倉庫（ID: 1）→ プリンタ1で印刷
curl -X POST https://tani-hub.sakemaru.click/api/printer/driver \
  -H "Authorization: Bearer {API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "warehouse_id": 1,
    "printer_id": 1,
    "print_driver_name": "Tokyo Warehouse Printer"
  }'

# 大阪倉庫（ID: 2）→ プリンタ2で印刷
curl -X POST https://tani-hub.sakemaru.click/api/printer/driver \
  -H "Authorization: Bearer {API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "warehouse_id": 2,
    "printer_id": 2,
    "print_driver_name": "Osaka Warehouse Printer"
  }'
```

### ローカルシステム側の設定

#### オプション1: 特定の倉庫のみ印刷

1. 設定画面を開く
2. **「倉庫一覧を読み込み」**ボタンをクリック
3. ドロップダウンから対象の倉庫を選択
   - 例: 「東京倉庫 (ID: 1)」を選択
4. プリンター設定を確認
   - プリンタ2が正しく設定されているか確認
5. **「保存」**ボタンをクリック

→ この設定により、東京倉庫の印刷ジョブのみが取得され、プリンタ2で印刷されます。

#### オプション2: 全倉庫の印刷ジョブを処理

1. 設定画面を開く
2. 倉庫選択を**「（全倉庫）」**のままにする
3. プリンター1〜4すべてを設定
4. **「保存」**ボタンをクリック

→ すべての倉庫の印刷ジョブが取得され、サーバー側で設定された`printer_id`に従って印刷されます。

### 設定例

#### 例1: 単一倉庫・単一プリンター

**環境:**
- 東京倉庫のみ運用
- プリンターは1台のみ

**設定:**
- サーバー側: `warehouse_id: 1, printer_id: 0`
- ローカル: 倉庫選択「東京倉庫」、プリンタ1のみ設定

#### 例2: 複数倉庫・倉庫ごとに専用プリンター

**環境:**
- 東京倉庫と大阪倉庫を運用
- 各倉庫に専用プリンターを設置

**設定:**
- サーバー側:
  - 東京倉庫: `warehouse_id: 1, printer_id: 1`
  - 大阪倉庫: `warehouse_id: 2, printer_id: 2`
- ローカル: 倉庫選択「（全倉庫）」、プリンタ2とプリンタ3を設定

#### 例3: 複数倉庫・共通プリンター

**環境:**
- 複数倉庫を運用
- プリンターは1台のみ（共用）

**設定:**
- サーバー側: すべての倉庫で`printer_id: 0`
- ローカル: 倉庫選択「（全倉庫）」、プリンタ1のみ設定

---

## 運用開始

### 1. アプリケーション起動

- Windowsの場合、システム起動時に自動的に開始されます
- タスクトレイにアイコンが表示されます

### 2. ポーリング開始

- 設定が正しく行われていれば、自動的にポーリングが開始されます
- ホーム画面で「ポーリング状態: 動作中」と表示されていることを確認

### 3. 動作確認

1. サーバー側で印刷ジョブを作成
2. ローカルシステムのログを確認
   - 「〇個の印刷ジョブを受信しました」と表示される
   - 「印刷開始」→「印刷完了」のログが表示される
3. プリンターから出力されることを確認

---

## トラブルシューティング

### 印刷されない

**確認事項:**
1. ポーリングが動作中か確認
2. API接続が正常か確認（API接続テスト）
3. プリンター設定が正しいか確認
4. ログにエラーメッセージがないか確認

**よくある原因:**
- プリンター番号の不一致（サーバー側とローカル側）
- プリンターがオフラインまたは認識されていない
- S3設定が間違っている

### 倉庫一覧が読み込めない

**確認事項:**
1. APIホストが正しいか
2. APIトークンが正しいか
3. サーバーへのHTTPS接続が可能か（ファイアウォール設定）

### プリンター番号の確認方法

**サーバー側の設定を確認:**
```bash
curl -X POST https://tani-hub.sakemaru.click/api/printer/warehouses \
  -H "Authorization: Bearer {API_TOKEN}" \
  -H "Content-Type: application/json"
```

レスポンスで各倉庫の`printer_id`を確認できます。

### ログの確認

- ホーム画面のログエリアで直近のログを確認
- 詳細なログは、以下のディレクトリに保存されています:
  - Windows: `C:\Users\{ユーザー名}\AppData\Roaming\Sakemaru\logs\`
  - macOS: `~/Library/Application Support/Sakemaru/logs/`
  - Linux: `~/.config/Sakemaru/logs/`

### サポート

問題が解決しない場合は、以下の情報とともにサポートに連絡してください:
1. エラーメッセージのスクリーンショット
2. ログファイル（最新日付のもの）
3. 設定内容（トークンを除く）
4. 使用しているOSとバージョン

---

## 付録: 設定ファイルの場所

設定は以下のファイルに保存されています:

- Windows: `C:\Users\{ユーザー名}\AppData\Roaming\Sakemaru\config.json`
- macOS: `~/Library/Application Support/Sakemaru/config.json`
- Linux: `~/.config/Sakemaru/config.json`

**注意:** 手動で編集する場合は、アプリケーションを終了してから行ってください。
