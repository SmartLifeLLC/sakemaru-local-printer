目的：複数プリンタに対して印刷ができるようにする。


現状はプリンタを一つしか設定できない。
1. プリンタは最大４つまで設定できるようにする。（設定画面の変更も必要）
2. ポーリング時にはプリンタの番号の指定ができるようにする。
3. 指定したプリンタへの印刷ができるようにする。
4. 指定した番号のプリンターの設定がない場合には1番のプリンタを利用する。


指示2
1.起動時のホーム画面にもプリンタの設定ができるようにする。
2.デザインを改修。よりみやすく、大きく表示する。
3.プリンタの設定はPCの再起動後にも設定が維持できるようにする。

指示３
1.  BZCore / BoozeCore をすべて廃止   「酒まる」「酒まる印刷」と表示
2. ホーム画面縦に長いので２列表示にする。

指示４
1. sample.pdfは設定で設定されたs3bucketの次のURLより取得する。
local_print_test/sample.pdf
2. 設定画面をホーム画面と同じように修正する。なまえは「酒まる通信設定」
3. 設定画面上のプリンタ設定はなくす。
4. 設定を別windowではなく、ホーム画面でタブ区切りで表示する。

指示５
印刷取得APIの改修。
1. サーバからは印刷対象のリストをもらうレスポンスは次のようになる。[[file_url:'xxx.pdf',printer_id:'1','file_id':'1','order':1],[file_url:'xxx2.pdf',printer_id:'2','file_id':'3','order':2]] のようにくる。
2. order順番通りにダウンロードと印刷を実行するひつようがある。
3. 印刷のリクエストが完了するまで次の印刷確認APIのリクエストは実行しない。
4. ファイルのダウンロードは最大4個並列でだうんろーどする。ダウンロードが完了した順番と関係なしにorder順に印刷する必要がある。

指示６
1. ホームタブを「プリンタ設定」に変更
2. 新しいホームタブを作成
3. ホームタブの機能
- pollingの開始・停止
- 動作中か停止中か
- 最終サーバapi取得時刻の表示
- エラーが発生した場合のログの表示(scroll必要)