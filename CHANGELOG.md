# 変更履歴

## v2.0.0 - 2025-11-18

### 新機能: 倉庫別プリンター印刷

倉庫ごとに異なるプリンターで印刷できるようになりました。

#### 主な変更点

1. **倉庫IDフィルター機能**
   - 設定画面で倉庫を指定できるようになりました
   - 指定した倉庫の印刷ジョブのみを取得します
   - 未指定の場合は全倉庫のジョブを取得します（従来通り）

2. **サーバー側でプリンター番号を管理**
   - 各印刷ジョブには、サーバー側で設定された`printer_id`（0-3）が含まれます
   - ローカルシステムは、この`printer_id`に対応するプリンターで印刷します
   - 倉庫ごとに異なるプリンター番号を設定できます

3. **自動ステータス更新**
   - 印刷完了後、自動的にサーバーへステータス更新（END）を送信します
   - 印刷失敗時は、エラーログに記録されます

#### 設定方法

##### 1. サーバー側の設定

サーバー側で倉庫ごとにプリンター設定を行います：

```json
POST https://{API_HOST}/api/printer/driver
Authorization: Bearer {API_TOKEN}
Content-Type: application/json

{
  "warehouse_id": 5,
  "printer_id": 1,
  "print_driver_name": "Main Printer"
}
```

**パラメータ説明:**
- `warehouse_id`: 倉庫ID（必須）
- `printer_id`: ローカルプリンター番号 0-3（省略可、デフォルト: 0）
  - 0: `printer0` (デフォルトプリンター)
  - 1: `printer1`
  - 2: `printer2`
  - 3: `printer3`
- `print_driver_name`: プリンター名（必須）

##### 2. ローカルプリンターシステムの設定

1. **倉庫の選択**
   - 設定画面を開く
   - 「倉庫一覧を読み込み」ボタンをクリック
   - ドロップダウンから対象の倉庫を選択
   - 未選択（全倉庫）のままでも動作します

2. **プリンターの設定**
   - 「プリンタ一覧を読み込み」ボタンをクリック
   - プリンタ1〜4を設定
   - サーバー側の`printer_id`と対応するプリンターを設定してください

3. **設定を保存**
   - 「保存」ボタンをクリック
   - ポーリングが自動的に再起動されます

#### 動作フロー

```
[サーバー] → [ポーリングAPI]
              ↓ warehouse_id指定
           [印刷ジョブ取得]
              ↓ printer_id含む
         [PDFダウンロード]
              ↓
      [指定プリンターで印刷]
              ↓
       [ステータス更新 (END)]
```

#### APIレスポンス例

```json
{
  "success": true,
  "data": {
    "data": [
      {
        "id": 123,
        "print_type": "slip",
        "file_path": "exports/slip_20250118.pdf",
        "printer_driver_id": 5,
        "printer_drivers": {
          "print_driver_name": "HP LaserJet",
          "warehouse_id": 1,
          "printer_id": 1,
          "warehouse": {
            "id": 1,
            "code": 1001,
            "name": "東京倉庫"
          }
        },
        "status": "STANDBY"
      }
    ]
  }
}
```

#### 互換性

- 既存の設定ファイルは自動的に移行されます
- `warehouse_id`が未設定の場合、全倉庫のジョブを取得します（v1.x互換）
- `printer_id`が未設定のジョブは、`printer0`で印刷されます

#### トラブルシューティング

**Q: 倉庫一覧が読み込めない**
- APIホストとAPIトークンが正しく設定されているか確認してください
- サーバーへの接続を確認してください（「API接続テスト」ボタン）

**Q: 印刷されない**
- サーバー側で正しい`printer_id`が設定されているか確認してください
- ローカルシステムの`printer0`〜`printer3`が正しく設定されているか確認してください
- ログを確認してください（ホーム画面のログ表示エリア）

**Q: 特定の倉庫のジョブのみ印刷したい**
- 設定画面で該当する倉庫を選択してください
- 保存後、その倉庫のジョブのみがポーリングで取得されます

---

## v1.x（以前のバージョン）

以前のバージョンでは、倉庫の概念がなく、すべての印刷ジョブを取得していました。
